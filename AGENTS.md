# 脚本工作流项目规则

本规则适用于“脚本工作流”项目下的所有任务和对话。

## 视频处理默认流程

- 用户上传视频后，默认依次执行：原文提取、中文翻译、原创改写、结构分析。
- 只有用户明确提出“只提取”或同义要求时，才只输出视频原文。
- 翻译要忠实；改写要避免复制原句，不虚构产品事实；分析要区分事实、推断和建议。
- 优先使用本地 `faster-whisper` 和本机 Ollama，不调用 OpenAI API。

## 数据与合规

- 用户视频、音频、密钥和私密配置不得上传 GitHub。
- 不读取、打印或提交 `.env.local` 等密钥文件。
- TikTok 只使用公开页面或公开 Creative Center 数据，不绕过登录、地区限制或反爬机制。
- 他人文案只做摘要、结构分析和原创改写，不复制完整文案。

## 项目交付

- 工作流代码、必要的规则和不含敏感信息的报告默认归档到 George 仓库。
- 修改后运行适配的语法、数据格式、相似度和合规检查。
- 推送前检查 Git 状态，确认没有视频文件、密钥或浏览器缓存。

## 饰品独立站上品快速流程

- 先在本地一次性完成 SKU 资料、尺寸图字段、标题风险闸门、标题候选、标签和图片清单，形成上品包；不要为每个字段单独往返读取页面。
- 没有尺寸图、尺寸字段不完整或风险无法确认时，立即暂停并输出提醒，不打开或提交后台表单。
- 后台操作采用“打开表单 → 批量填写 → 一次校验 → 提交 → 立即下架”的节奏；只有提交前和提交后各做一次状态核验。
- 浏览器连接异常时停止重试，先报告具体阻塞点；不得用新会话替代用户已登录会话，避免重新登录和重复等待。
- 标题 skill 输出的 `status`、`risk_summary`、`new_keywords`、`notes` 必须随上品包保存，不能只保留标题文本。
- 单 SKU 目标时长控制在 10 分钟内：资料与图片检查 2 分钟、标题与风险 1 分钟、缺图生成 4 分钟、后台批量填写提交 3 分钟；超过阶段上限立即报告阻塞，不无限等待。
