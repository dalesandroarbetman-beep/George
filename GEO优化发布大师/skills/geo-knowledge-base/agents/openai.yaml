interface:
  display_name: "GEO知识库"
  short_description: "维护可追溯的品牌与产品事实卡"
  default_prompt: "Use $geo-knowledge-base to convert these site materials into versioned fact cards and flag conflicts before content writing."
policy:
  allow_implicit_invocation: true
